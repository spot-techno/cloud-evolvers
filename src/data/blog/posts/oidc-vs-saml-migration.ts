import type { BlogPost } from '../types';

export const oidcVsSamlMigrationPost: BlogPost = {
  id: 'oidc-vs-saml-when-to-use-which',
  title: {
    en: 'OIDC vs SAML vs WS-Fed: When to Use Which Protocol in Entra ID',
    nl: 'OIDC vs SAML vs WS-Fed: Welk Protocol Wanneer Gebruiken in Entra ID'
  },
  description: {
    en: 'A practical comparison of OIDC, SAML 2.0, and WS-Federation in Microsoft Entra ID, when to pick each protocol and how to migrate from ADFS to Entra ID cloud authentication.',
    nl: 'Een praktische vergelijking van OIDC, SAML 2.0 en WS-Federation in Microsoft Entra ID, wanneer elk protocol te kiezen en hoe te migreren van ADFS naar Entra ID cloud authenticatie.'
  },
  date: '2026-01-18',
  author: 'Yair Knijn',
  tags: ['Entra ID', 'OIDC', 'SAML', 'WS-Fed', 'Identity', 'Migration'],
  image: '/images/unsplash/api-integration.jpg',
  excerpt: {
    en: 'SAML for legacy enterprise apps, OIDC for modern development, WS-Fed only when migrating from ADFS. Covers protocol differences, token formats, the ADFS-to-Entra migration path, and common pitfalls.',
    nl: 'SAML voor legacy enterprise apps, OIDC voor moderne ontwikkeling, WS-Fed alleen bij migratie van ADFS. Behandelt protocolverschillen, tokenformaten, het ADFS-naar-Entra migratiepad en veelvoorkomende valkuilen.'
  },
  category: {
    en: 'Azure Security',
    nl: 'Azure Beveiliging'
  },
  readTime: 8,
  content: {
    introduction: {
      en: 'Building something new? Use OIDC. That is the short answer. The longer answer is that you probably have a pile of legacy apps using SAML 2.0, a few ADFS relying party trusts on WS-Federation, and at least one custom integration that will break if you look at it wrong. Entra ID supports all three protocols, but picking the right one per app, and knowing how to migrate off ADFS without an outage, is where most teams get stuck. Here is a practical breakdown of when to use which protocol and how the ADFS-to-Entra migration actually works.',
      nl: 'Bouw je iets nieuws? Gebruik OIDC. Dat is het korte antwoord. Het langere antwoord is dat je waarschijnlijk een stapel legacy-apps hebt op SAML 2.0, een paar ADFS relying party trusts op WS-Federation, en minstens een custom integratie die kapotgaat als je er scheef naar kijkt. Entra ID ondersteunt alle drie de protocollen, maar het juiste kiezen per app, en weten hoe je van ADFS af migreert zonder downtime, is waar de meeste teams vastlopen. Hier is een praktisch overzicht van wanneer welk protocol te gebruiken en hoe de ADFS-naar-Entra migratie echt werkt.'
    },
    sections: [
      {
        title: {
          en: 'Protocol Comparison: SAML 2.0, OIDC, and WS-Federation',
          nl: 'Protocolvergelijking: SAML 2.0, OIDC en WS-Federation'
        },
        content: {
          en: 'SAML 2.0 has been the standard for enterprise SSO since the mid-2000s. It uses XML-based assertions, communicates via browser redirects (HTTP-POST and HTTP-Redirect bindings), and is well-supported by virtually every enterprise application, ServiceNow, Salesforce, SAP, Workday, and hundreds of others. The SAML assertion is a signed XML document containing the user\'s identity attributes (claims). The assertion goes from the Identity Provider (Entra ID) to the Service Provider (the application) via the user\'s browser. SAML is verbose (XML is larger than JSON), does not have a native concept of API authorization (it was designed for browser-based SSO), and its implementation complexity leads to common configuration errors around assertion consumer service URLs and certificate management. OIDC (OpenID Connect) is built on top of OAuth 2.0 and uses JSON Web Tokens (JWTs). It supports browser-based SSO like SAML but also handles API authorization, single-page applications, mobile apps, and machine-to-machine flows via the same framework. Tokens are smaller (JSON vs XML), the protocol is simpler to implement, and MSAL (Microsoft Authentication Library) provides battle-tested client libraries for every major platform. OIDC is what Microsoft uses for its own applications and is the recommended protocol for all new development against Entra ID. WS-Federation is an older XML-based protocol primarily associated with ADFS and older ASP.NET applications. In Entra ID, it is used when you have applications that were originally configured as ADFS relying party trusts and use WS-Fed for authentication. Microsoft 365 clients internally use WS-Fed in some legacy flows, though modern auth has largely replaced this. Unless you are specifically migrating from ADFS, you will rarely choose WS-Fed for a new integration.',
          nl: 'SAML 2.0 is de standaard voor enterprise SSO sinds het midden van de jaren 2000. Het gebruikt XML-gebaseerde assertions, communiceert via browser redirects (HTTP-POST en HTTP-Redirect bindings), en wordt ondersteund door vrijwel elke enterprise applicatie, ServiceNow, Salesforce, SAP, Workday en honderden andere. De SAML assertion is een ondertekend XML-document met de identiteitsattributen (claims) van de gebruiker. De assertion gaat van de Identity Provider (Entra ID) naar de Service Provider (de applicatie) via de browser van de gebruiker. SAML is verbose (XML is groter dan JSON), heeft geen native concept van API-autorisatie (het is ontworpen voor browser-gebaseerde SSO), en de implementatiecomplexiteit leidt tot veelvoorkomende configuratiefouten rond assertion consumer service URL\'s en certificaatbeheer. OIDC (OpenID Connect) is gebouwd bovenop OAuth 2.0 en gebruikt JSON Web Tokens (JWT\'s). Het ondersteunt browser-gebaseerde SSO zoals SAML maar handelt ook API-autorisatie, single-page applicaties, mobiele apps en machine-to-machine flows af via hetzelfde framework. Tokens zijn kleiner (JSON vs XML), het protocol is eenvoudiger te implementeren, en MSAL (Microsoft Authentication Library) biedt beproefde clientbibliotheken voor elk belangrijk platform. OIDC is wat Microsoft gebruikt voor zijn eigen applicaties en is het aanbevolen protocol voor alle nieuwe ontwikkeling tegen Entra ID. WS-Federation is een ouder XML-gebaseerd protocol dat voornamelijk wordt geassocieerd met ADFS en oudere ASP.NET applicaties. In Entra ID wordt het gebruikt wanneer je applicaties hebt die oorspronkelijk geconfigureerd waren als ADFS relying party trusts en WS-Fed gebruiken voor authenticatie. Microsoft 365 clients gebruiken intern WS-Fed in sommige legacy flows, hoewel moderne auth dit grotendeels heeft vervangen. Tenzij je specifiek migreert van ADFS, zul je zelden WS-Fed kiezen voor een nieuwe integratie.'
        }
      },
      {
        title: {
          en: 'When to Use Each Protocol',
          nl: 'Wanneer Elk Protocol Gebruiken'
        },
        content: {
          en: 'Use OIDC when: you are building a new application (web app, SPA, mobile app, API), the application supports modern authentication, you need API authorization alongside SSO (OAuth 2.0 scopes and access tokens), or you want to use MSAL libraries for a consistent developer experience. Entra ID app registrations default to OIDC, and the MSAL libraries handle token acquisition, caching, and renewal automatically. Use SAML 2.0 when: the application only supports SAML (many enterprise SaaS applications fall into this category), you are integrating a third-party application from the Entra ID gallery that is configured for SAML, or you need to federate with an external identity provider that only speaks SAML. In Entra ID, SAML SSO is configured through Enterprise Applications > Single sign-on > SAML. You configure the Entity ID, Assertion Consumer Service URL, and the claims to include in the SAML assertion. Use WS-Federation when: you are migrating applications from ADFS that were configured as WS-Fed relying party trusts and cannot be reconfigured to use OIDC or SAML. Some older ASP.NET applications use the WIF (Windows Identity Foundation) framework and expect WS-Fed tokens. If possible, update these applications to use MSAL and OIDC, but if that is not feasible in the short term, Entra ID supports WS-Fed as a migration path.',
          nl: 'Gebruik OIDC wanneer: je een nieuwe applicatie bouwt (web app, SPA, mobiele app, API), de applicatie moderne authenticatie ondersteunt, je API-autorisatie naast SSO nodig hebt (OAuth 2.0 scopes en access tokens), of je MSAL-bibliotheken wilt gebruiken voor een consistente developer experience. Entra ID app registrations gebruiken standaard OIDC, en de MSAL-bibliotheken handelen token acquisition, caching en renewal automatisch af. Gebruik SAML 2.0 wanneer: de applicatie alleen SAML ondersteunt (veel enterprise SaaS applicaties vallen in deze categorie), je een derde-partij applicatie uit de Entra ID gallery integreert die geconfigureerd is voor SAML, of je moet federeren met een externe identity provider die alleen SAML spreekt. In Entra ID wordt SAML SSO geconfigureerd via Enterprise Applications > Single sign-on > SAML. Je configureert de Entity ID, Assertion Consumer Service URL en de claims om op te nemen in de SAML assertion. Gebruik WS-Federation wanneer: je applicaties migreert van ADFS die geconfigureerd waren als WS-Fed relying party trusts en niet geconfigureerd kunnen worden om OIDC of SAML te gebruiken. Sommige oudere ASP.NET applicaties gebruiken het WIF (Windows Identity Foundation) framework en verwachten WS-Fed tokens. Indien mogelijk, update deze applicaties naar MSAL en OIDC, maar als dat op korte termijn niet haalbaar is, ondersteunt Entra ID WS-Fed als migratiepad.'
        }
      },
      {
        title: {
          en: 'The ADFS to Entra ID Migration Path',
          nl: 'Het ADFS naar Entra ID Migratiepad'
        },
        content: {
          en: 'Microsoft provides the AD FS application activity report in the Entra admin center (under Identity > Applications > AD FS application activity) that analyzes your ADFS relying party trusts and tells you which applications are ready to migrate to Entra ID and which require additional work. The report checks for compatibility issues: custom issuance transform rules that use ADFS-specific functions, unsupported claim types, or configurations that have no direct Entra ID equivalent. For applications that are migration-ready, the process is: create an Enterprise Application in Entra ID (either from the gallery or a custom SAML/OIDC integration), configure the SSO settings to match the existing ADFS relying party trust, translate the ADFS issuance rules to Entra claims mapping policies, and then use staged rollout to move users in batches. Staged rollout lets you redirect specific users or groups from ADFS to Entra ID for authentication while the rest continue using ADFS. This is controlled via the Entra admin center under Identity > Hybrid management > Microsoft Entra Connect > Staged Rollout. You enable it per feature (password hash sync, pass-through authentication, seamless SSO) and assign groups of users to test the cloud authentication path. This gives you a safe way to validate each application migration without a big-bang cutover.',
          nl: 'Microsoft biedt het AD FS application activity report in het Entra admin center (onder Identity > Applications > AD FS application activity) dat je ADFS relying party trusts analyseert en je vertelt welke applicaties klaar zijn om te migreren naar Entra ID en welke extra werk vereisen. Het rapport controleert op compatibiliteitsproblemen: custom issuance transform rules die ADFS-specifieke functies gebruiken, niet-ondersteunde claimtypen, of configuraties die geen directe Entra ID equivalent hebben. Voor applicaties die migratieklaar zijn, is het proces: maak een Enterprise Application in Entra ID aan (vanuit de gallery of een custom SAML/OIDC integratie), configureer de SSO-instellingen om overeen te komen met de bestaande ADFS relying party trust, vertaal de ADFS issuance rules naar Entra claims mapping policies, en gebruik dan staged rollout om gebruikers in batches te verplaatsen. Staged rollout laat je specifieke gebruikers of groepen van ADFS naar Entra ID doorsturen voor authenticatie terwijl de rest ADFS blijft gebruiken. Dit wordt beheerd via het Entra admin center onder Identity > Hybrid management > Microsoft Entra Connect > Staged Rollout. Je schakelt het per functie in (password hash sync, pass-through authentication, seamless SSO) en wijst groepen gebruikers toe om het cloud authenticatiepad te testen. Dit geeft je een veilige manier om elke applicatiemigratie te valideren zonder een big-bang cutover.'
        }
      },
      {
        title: {
          en: 'Claims Rule Translation: ADFS to Entra ID',
          nl: 'Claims Rule Vertaling: ADFS naar Entra ID'
        },
        content: {
          en: 'The most time-consuming part of ADFS migration is translating issuance transform rules to Entra ID claims mapping. ADFS uses a custom rule language for claims manipulation, conditions, value transformations, and multi-step issuance logic. Entra ID has a different mechanism: claims mapping policies (applied via PowerShell or Graph API to service principals) and the claims customization UI in the portal. Common translations: the ADFS rule "c:[Type == \"http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress\"] => issue(claim = c);" becomes simply mapping the user.mail attribute to the email claim in Entra. Group-based claims in ADFS (issuing a role claim based on group membership) map to the groups claim configuration in Entra token settings or to app roles assigned to groups. String transformation rules in ADFS (like RegexReplace or string concatenation) can be handled by claims transformation policies in Entra, though the syntax is different and some complex transformations may require Azure Functions as a custom claims provider. Claims that exist in ADFS but have no direct Entra equivalent include: custom claims derived from SQL attribute stores, LDAP attribute store lookups against non-AD directories, and claims based on incoming claims from other claims providers chained in ADFS. For these, you need a custom claims provider, an API endpoint that Entra calls during token issuance via custom authentication extensions (the token issuance start event). This API receives the current token claims and returns additional claims to include.',
          nl: 'Het meest tijdrovende deel van ADFS-migratie is het vertalen van issuance transform rules naar Entra ID claims mapping. ADFS gebruikt een custom rule language voor claims-manipulatie, voorwaarden, waarde-transformaties en meerstaps issuance-logica. Entra ID heeft een ander mechanisme: claims mapping policies (toegepast via PowerShell of Graph API op service principals) en de claims customization UI in de portal. Veelvoorkomende vertalingen: de ADFS-regel "c:[Type == \\"http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress\\"] => issue(claim = c);" wordt simpelweg het mappen van het user.mail attribuut naar de email claim in Entra. Groep-gebaseerde claims in ADFS (een rolclaim uitgeven op basis van groepslidmaatschap) mappen naar de groups claim configuratie in Entra token settings of naar app rollen toegewezen aan groepen. String transformatie regels in ADFS (zoals RegexReplace of string concatenatie) kunnen worden afgehandeld door claims transformation policies in Entra, hoewel de syntax verschilt en sommige complexe transformaties Azure Functions als custom claims provider nodig kunnen hebben. Claims die bestaan in ADFS maar geen directe Entra equivalent hebben zijn: custom claims afgeleid van SQL attribute stores, LDAP attribute store lookups tegen non-AD directories, en claims gebaseerd op inkomende claims van andere claims providers gekoppeld in ADFS. Hiervoor heb je een custom claims provider nodig, een API-endpoint dat Entra aanroept tijdens token issuance via custom authentication extensions (het token issuance start event). Deze API ontvangt de huidige token claims en retourneert aanvullende claims om op te nemen.'
        }
      },
      {
        title: {
          en: 'Common Migration Pitfalls',
          nl: 'Veelvoorkomende Migratie Valkuilen'
        },
        content: {
          en: 'Several issues consistently trip up ADFS-to-Entra migrations. First: redirect URI changes. ADFS and Entra ID may use different endpoint URLs, and applications that had the ADFS federation metadata URL hardcoded will break. Update the application\'s identity provider configuration to point to Entra ID endpoints (https://login.microsoftonline.com/{tenant-id}/v2.0/.well-known/openid-configuration for OIDC, or the federation metadata XML at https://login.microsoftonline.com/{tenant-id}/federationmetadata/2007-06/federationmetadata.xml for SAML/WS-Fed). Second: implicit grant flow deprecation. Older SPAs configured in ADFS may use the OAuth implicit grant to get access tokens directly from the authorization endpoint. Entra ID v2.0 endpoints have deprecated implicit grant for access tokens, you must use the authorization code flow with PKCE instead. This requires changes to the frontend application code if it was built with older libraries like ADAL.js. Third: the NameIdentifier claim format. ADFS applications often depend on a specific NameID format (like email address or persistent identifier). Entra ID\'s default NameIdentifier for SAML is the user\'s objectId. If your application expects email format, you need to configure the NameID claim in the SAML configuration to use user.userprincipalname or user.mail. Fourth: signing certificate rotation. ADFS typically uses long-lived token signing certificates. Entra ID automatically rotates its signing keys. Applications that have the signing certificate pinned (rather than dynamically fetching it from the metadata endpoint) will break on rotation. Ensure your applications fetch the signing keys from the metadata endpoint or use the JWKS URI dynamically.',
          nl: 'Meerdere problemen laten ADFS-naar-Entra migraties consistent struikelen. Ten eerste: redirect URI wijzigingen. ADFS en Entra ID kunnen verschillende endpoint-URL\'s gebruiken, en applicaties met de ADFS federation metadata URL hardcoded zullen breken. Update de identity provider configuratie van de applicatie om te verwijzen naar Entra ID endpoints (https://login.microsoftonline.com/{tenant-id}/v2.0/.well-known/openid-configuration voor OIDC, of de federation metadata XML op https://login.microsoftonline.com/{tenant-id}/federationmetadata/2007-06/federationmetadata.xml voor SAML/WS-Fed). Ten tweede: implicit grant flow afschaffing. Oudere SPA\'s geconfigureerd in ADFS gebruiken mogelijk de OAuth implicit grant om access tokens direct van het authorization endpoint te krijgen. Entra ID v2.0 endpoints hebben implicit grant voor access tokens afgeschaft, je moet in plaats daarvan de authorization code flow met PKCE gebruiken. Dit vereist wijzigingen in de frontend applicatiecode als deze gebouwd was met oudere bibliotheken zoals ADAL.js. Ten derde: het NameIdentifier claim format. ADFS-applicaties zijn vaak afhankelijk van een specifiek NameID-formaat (zoals e-mailadres of persistent identifier). Entra ID\'s standaard NameIdentifier voor SAML is de objectId van de gebruiker. Als je applicatie e-mailformaat verwacht, moet je de NameID claim in de SAML-configuratie configureren om user.userprincipalname of user.mail te gebruiken. Ten vierde: signing certificate rotatie. ADFS gebruikt typisch langlevende token signing certificates. Entra ID roteert automatisch zijn signing keys. Applicaties met het signing certificate gepind (in plaats van het dynamisch ophalen van het metadata endpoint) zullen breken bij rotatie. Zorg ervoor dat je applicaties de signing keys ophalen van het metadata endpoint of de JWKS URI dynamisch gebruiken.'
        }
      }
    ],
    conclusion: {
      en: 'The decision tree is simple. New app: OIDC, use MSAL, done. Existing enterprise SaaS that only speaks SAML: configure SAML SSO in Entra ID, it works fine. Legacy ADFS app on WS-Fed that nobody wants to touch: leave it on WS-Fed in Entra ID for now, plan the OIDC migration later. If you are migrating off ADFS, run the application activity report first, it tells you exactly which apps are ready and which need work. Use staged rollout to move users in batches, not all at once. And triple-check your redirect URIs, NameID format, and certificate rotation setup. Those three cause most of the post-migration outages we see.',
      nl: 'De beslisboom is simpel. Nieuwe app: OIDC, gebruik MSAL, klaar. Bestaande enterprise SaaS die alleen SAML spreekt: configureer SAML SSO in Entra ID, werkt prima. Legacy ADFS-app op WS-Fed waar niemand aan wil zitten: laat het op WS-Fed in Entra ID voor nu, plan de OIDC-migratie later. Als je van ADFS af migreert, draai eerst het application activity report, dat vertelt je precies welke apps klaar zijn en welke werk nodig hebben. Gebruik staged rollout om gebruikers in batches te verplaatsen, niet allemaal tegelijk. En controleer drie keer je redirect URI\'s, NameID-formaat en certificaatrotatie-instellingen. Die drie veroorzaken de meeste post-migratie-uitval die we zien.'
    }
  }
};
